import { CSSProperties } from "react";

const style = {
  loaderWrapper: {
    display: 'flex',
    justifyContent: 'center',
    marginTop: 10
  } as CSSProperties,
  streamWrapper: {
    display: 'flex',
    justifyContent: 'center',    
    marginTop: 10
  } as CSSProperties
}

export default style