import { CSSProperties } from "react";

const style = {
  loaderWrapper: {
    display: 'flex',
    justifyContent: 'center',
    marginTop: 10
  } as CSSProperties,
  streamWrapper: {
    marginTop: 10,
    maxWidth: '100%'
  } as CSSProperties
}

export default style